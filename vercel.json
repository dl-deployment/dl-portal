{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install && cd api && npm install",
  "rewrites": [
    { "source": "/((?!api/|static_pages/).*)", "destination": "/index.html" }
  ]
}
